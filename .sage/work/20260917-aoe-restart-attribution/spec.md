---
status: completed
phase: specification
tracker: CRA-469
---
# AOE restart observation contract

The user approved the four crash-attribution plans and autonomous implementation; parent approved this AOE adapter design. Before teardown, best-effort record prior Ledger run, prior native session and requested resume/fresh intent. Carry the returned intent into the new launch. Native source-qualified ingestion, never AOE pane liveness, determines verified resume. Preserve existing launch, fallback and cancellation behavior. Keep existing strict launch-identity report unchanged and add a separate optional ownership-checked pane metadata report. No persisted AOE schema, live deployment, provider calls or credential/payload retention.
